name: Build Android APK

# DISABLED 2026-07-07 — vext CI + release moved to the monorepo
# (leonx03/apps-monorepo → .github/workflows/vext-build.yml). Reduced to workflow_dispatch-only +
# `if: false` on the job so nothing fires here automatically (never let two pipelines build the same
# app). To reactivate: restore the push/tags/issue_comment triggers and remove `if: false` below.
#
# Original triggers (for reference / rollback):
#   push: { branches: [master], tags: ['v*'] }
#   workflow_dispatch: {}
#   issue_comment: { types: [created] }   # "/build" comment → test build
on:
  workflow_dispatch:

permissions: {}

jobs:
  build:
    name: Build APK
    runs-on: ubuntu-latest
    if: false # DISABLED — vext moved to monorepo (apps-monorepo/.github/workflows/vext-build.yml)
    permissions:
      contents: write
      pull-requests: read

    steps:
      # ── Determine which environment we're building for ──
      - name: Determine build environment
        id: env
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
            echo "environment=production" >> "$GITHUB_OUTPUT"
            echo "version=$VERSION" >> "$GITHUB_OUTPUT"
          elif [[ "$GITHUB_EVENT_NAME" == "workflow_dispatch" || ( "$GITHUB_EVENT_NAME" == "push" && "$GITHUB_REF" == "refs/heads/master" ) ]]; then
            echo "environment=staging" >> "$GITHUB_OUTPUT"
            echo "version=" >> "$GITHUB_OUTPUT"
          else
            echo "environment=test" >> "$GITHUB_OUTPUT"
            echo "version=" >> "$GITHUB_OUTPUT"
          fi

      # ── Resolve PR head SHA (only for /build comment trigger) ──
      - name: Get PR head ref
        if: github.event_name == 'issue_comment'
        id: pr
        uses: actions/github-script@v7
        with:
          script: |
            const pr = await github.rest.pulls.get({
              owner: context.repo.owner,
              repo: context.repo.repo,
              pull_number: context.issue.number,
            });
            core.setOutput('sha', pr.data.head.sha);

      # ── Setup ──
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'issue_comment' && steps.pr.outputs.sha || github.sha }}

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 9

      - name: Cache pnpm store
        uses: actions/cache@v4
        with:
          path: ~/.local/share/pnpm/store/v3
          key: pnpm-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml') }}
          restore-keys: pnpm-${{ runner.os }}-

      - name: Cache Gradle
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: gradle-${{ runner.os }}-${{ hashFiles('pnpm-lock.yaml', 'app.json') }}
          restore-keys: gradle-${{ runner.os }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      # ── Version (production only) ──
      - name: Update version from tag
        if: steps.env.outputs.environment == 'production' && steps.env.outputs.version != ''
        env:
          VERSION: ${{ steps.env.outputs.version }}
          VERSION_CODE: ${{ github.run_number }}
        run: |
          node -e "
            const fs = require('fs');
            const config = JSON.parse(fs.readFileSync('app.json', 'utf8'));
            config.expo.version = process.env.VERSION;
            config.expo.android = config.expo.android || {};
            config.expo.android.versionCode = parseInt(process.env.VERSION_CODE);
            fs.writeFileSync('app.json', JSON.stringify(config, null, 2) + '\n');
            console.log('Set version ' + process.env.VERSION + ' (code ' + process.env.VERSION_CODE + ')');
          "

      # ── App ID overrides ──
      - name: Set production application ID
        if: steps.env.outputs.environment == 'production'
        run: |
          node -e "
            const fs = require('fs');
            const config = JSON.parse(fs.readFileSync('app.json', 'utf8'));
            config.expo.name = 'Vext';
            config.expo.android = config.expo.android || {};
            config.expo.android.package = 'com.anonymous.vext';
            fs.writeFileSync('app.json', JSON.stringify(config, null, 2) + '\n');
            console.log('Set production app ID: com.anonymous.vext');
          "

      - name: Set staging application ID
        if: steps.env.outputs.environment == 'staging'
        run: |
          node -e "
            const fs = require('fs');
            const config = JSON.parse(fs.readFileSync('app.json', 'utf8'));
            config.expo.name = 'Vext Staging';
            config.expo.android = config.expo.android || {};
            config.expo.android.package = 'com.anonymous.vext.staging';
            fs.writeFileSync('app.json', JSON.stringify(config, null, 2) + '\n');
            console.log('Set staging app ID: com.anonymous.vext.staging');
          "

      # ── Generate native project ──
      - name: Expo prebuild
        run: npx expo prebuild --platform android --clean

      # ── Signing (staging/production only) ──
      - name: Decode release keystore
        if: steps.env.outputs.environment != 'test'
        env:
          RELEASE_KEYSTORE_BASE64: ${{ secrets.RELEASE_KEYSTORE_BASE64 }}
        run: echo "$RELEASE_KEYSTORE_BASE64" | base64 -d > android/app/vext-release.keystore

      # ── Build ──
      - name: Build release APK
        working-directory: android
        env:
          RELEASE_KEYSTORE_FILE: ${{ steps.env.outputs.environment != 'test' && 'vext-release.keystore' || '' }}
          RELEASE_KEYSTORE_PASSWORD: ${{ secrets.RELEASE_KEYSTORE_PASSWORD }}
          RELEASE_KEY_ALIAS: ${{ secrets.RELEASE_KEY_ALIAS }}
          RELEASE_KEY_PASSWORD: ${{ secrets.RELEASE_KEY_PASSWORD }}
        run: ./gradlew assembleRelease --no-daemon

      # ── Rename APK for clarity ──
      - name: Rename APK
        run: |
          mv android/app/build/outputs/apk/release/app-release.apk \
             android/app/build/outputs/apk/release/vext-${{ steps.env.outputs.environment }}.apk

      # ── Upload artifact (always) ──
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: vext-${{ steps.env.outputs.environment }}-apk
          path: android/app/build/outputs/apk/release/vext-${{ steps.env.outputs.environment }}.apk
          retention-days: 30

      # ── GitHub Release (production only) ──
      - name: Create GitHub Release
        if: steps.env.outputs.environment == 'production'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Vext v${{ steps.env.outputs.version }}
          generate_release_notes: true
          files: android/app/build/outputs/apk/release/vext-production.apk
