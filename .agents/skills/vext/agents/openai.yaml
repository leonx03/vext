interface:
  display_name: "Vext"
  short_description: "Repo-specific patterns and workflows for vext"
  default_prompt: "Use the vext repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true